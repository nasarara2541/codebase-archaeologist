# Preview lifecycle limits (milliseconds).
PREVIEW_START_TIMEOUT_MS=15000
PREVIEW_TTL_MS=300000
ANALYSIS_TTL_MS=600000

# Optional: raises GitHub's public REST API rate limit for read-only analysis.
GITHUB_TOKEN=

# Required for Checkpoint 4 grounded feature tracing.
OPENAI_API_KEY=
OPENAI_TRACE_MODEL=gpt-5.6-sol
